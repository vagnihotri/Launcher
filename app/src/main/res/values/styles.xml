<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Holo.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="LauncherTheme" parent="android:Theme.Material.Wallpaper.NoTitleBar">
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="CodeFont" parent="@android:style/Widget.Button">
        <item name="android:layout_width">fill_parent</item>
        <item name="android:layout_height">fill_parent</item>
        <item name="android:background">@drawable/lockscreen_button</item>
        <item name="android:textColor">#CCCCCC</item>
        <item name="android:textSize">35dip</item>
        <item name="android:gravity">center</item>
        <item name="android:padding">10dip</item>
        <item name="android:layout_marginTop">10dip</item>
        <item name="android:layout_marginLeft">5dip</item>
        <item name="android:layout_marginRight">5dip</item>
    </style>

</resources>
